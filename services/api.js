import axios from 'axios';

export const api = axios.create({
  baseURL: '',
  headers: {
    apikey:
      ,
    authorization:
      
  },
});

//É necessário incluir os tokens para que o projeto funcione corretamente